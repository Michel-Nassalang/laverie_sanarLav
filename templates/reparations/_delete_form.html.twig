<form method="post" action="{{ path('app_reparations_delete', {'id': reparation.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette réclammation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reparation.id) }}">
<button class="btn btn-primary py-3 px-5 mt-2">{% trans %}Supprimer{% endtrans %}</button>


</form>
